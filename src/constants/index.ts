// import { RadicalRedPokedex } from "@/types";

export const filters = ["Name", "Type", "Move", "Ability"] as const;

// export const species = data.species;
// export const typings = data.types;
// export const moves = data.moves;
